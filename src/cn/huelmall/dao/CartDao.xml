<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.huelmall.dao.CartDao">
	<!-- 根据pid查询商品 -->
	<select id="findProductByPid" parameterType="string" resultType="cn.huelmall.domain.Product">
	select * from product where pid=#{pid}
	</select>
</mapper>